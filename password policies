FILES:
    /etc/login.defs -> max days and warn days. also max retires but will be overridden by PAM
    /etc/pam.d/common-password -> set up further passwrod policies

libpam-quality

    check description
        apt show libpam-pwquality
    install
        sudo apt install libpam-pwquality 